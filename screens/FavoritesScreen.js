import { StyleSheet, View, Text } from "react-native";

const Favorites = () => {
	return (
		<View>
			<Text>Favorites</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default Favorites;
